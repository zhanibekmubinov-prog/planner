# HANDOFF — текущее состояние и следующий шаг

_Обновлено: 2026-09-08, сессия 17 (Cowork). **v1.3 — переезд из Trello: MCP `create_tasks_bulk`** (до 50 задач за вызов, всё-или-ничего, пропуск дублей по названию, `dry_run`; `tests/test_v13_bulk.py` 10 тестов, pytest 266) **+ `docs/TRELLO.md`** — инструкция коллегам. Без миграции. Ждёт коммита. Ранее в тот же день — сессия 16: **v1.2 — майндмап: цвет/толщина ветки, важность ! / !! / !!!, подписи линий, связи-стрелки между узлами** (`MindMapEditor.tsx`, `api.ts`, `styles.css`; vitest 77; без миграции). Ждёт `npm run build` + коммит. Ранее — сессия 15: **v1.1.1 — имена людей кириллицей ↔ латиницей** в MCP (`app/names.py`, хук `fuzzy` в `_match`, 49 тестов, pytest 258). Ждёт коммита. Ранее — сессия 14: **v1.1 — на телефоне «потянуть сверху вниз — обновить»** (`PullToRefresh.tsx`, `store.refresh()`, тесты 72). Ждёт `npm run build` + коммит. Ранее в тот же день — **версия 1.0 — планнер запущен в использование.** Иконка приложения «лого + PLANNER» (favicon, apple-touch-icon, maskable) и правильная подпись на домашнем экране; презентация — 21 слайд со слайдом про телефон и свежими инструкциями; для коллег подготовлена база знаний под отдельный проект claude.ai. Ранее: v0.11 — версия для телефона (нижние вкладки, доска одной колонкой, «⋯» меняет статус); v0.10 — гости с паролями (**нужны миграции `d5e6f7a8b9c0` и `e6f7a8b9c0d1`**); v0.9 — Telegram-бот и коннектор в ChatGPT; v0.8 — починка по итогам адверсариального ревью._
_Дополнение сессии 12 (после сессии 13): **экран входа переделан** — два подписанных входа («Сотрудник CIS» / «Внешний участник»), форма гостя видна сразу; повод — гость с gmail нажал кнопку Microsoft и получил AADSTS50020. Файлы `Account.tsx`, `styles.css`, `__tests__/login.test.tsx`; vitest 67. **Ждёт коммита/пуша.** Гостю объяснить: нижний блок «Внешний участник», кнопку Microsoft не трогать; первый раз — «прислать ссылку на почту»._

## Состояние
| Что | Статус |
|---|---|
| Railway `postgres` | ✅ |
| Railway `backend` | ✅ https://backend-production-830f1.up.railway.app — `/health` ok, `/docs` с кнопкой Authorize |
| Railway `frontend` | ✅ https://cis-planner.up.railway.app — деплой зелёный |
| `CORS_ORIGINS` у backend | ✅ `https://cis-planner.up.railway.app,http://localhost:5173` |
| Переменные frontend `VITE_API_URL`, `VITE_API_TOKEN` | ✅ заданы |
| Шаг 2 — фронт MVP | ✅ v0.2 в проде; тема «Инженерный журнал» (бумага, Source Serif 4 заголовки, Rubik текст, Source Code Pro цифры), шрифты через `@fontsource` |
| Шаг 4 — планировщик напоминаний | ✅ `backend/app/scheduler.py` (цикл раз в 60 с внутри бэкенда), `notify.py` (Telegram Bot API; Microsoft Graph: sendMail + события календаря), `routers/notify.py` (`GET /api/notify/status`, `POST /api/notify/test {channel}`, `POST /api/notify/run-now`, `POST /api/notify/digest`) |
| Напоминания по поручениям | ✅ `Delegation.notified_at` (миграция `b7c1d2e3f4a5`), `scheduler.process_delegations` — «Пора проверить у X» владельцу |
| Утренняя сводка | ✅ `digest.py` + `scheduler.send_digest`; ежедневно в `DIGEST_TIME` (08:30 Asia/Oral) по `DIGEST_CHANNELS` |
| v0.3 Многопользовательский режим | ✅ `users` + `owner_id`, вход через Microsoft (`routers/auth.py`), JWT, видимость «своё + порученное», раздел «Мне поручено», профиль |
| Майндмапы | ✅ модель `MindMap` (миграция `c8d2e3f4a5b6`), роутер `/api/mindmaps`, редактор MindNode-стиля (`MindMapEditor.tsx`) |
| v0.4 | ✅ `Reminder.recipient` (миграция `e0f4a5b6c7d8`), сайдбар с разделами и списком направлений, `DirectionMenu.tsx`, страницы-списки по центру |
| v0.5 MCP-коннектор для Claude | ✅ в проде, владелец и коллеги подключены. `routers/mcp_oauth.py`, `routers/mcp.py`, `mcp_tools.py` (25 инструментов), миграция `f1a5b6c7d8e9`, `docs/MCP_CONNECTOR.md`, `test_mcp.py` |
| v0.6 Проекты + совместный доступ | ✅ закоммичено (`d78411e`), задеплоено; миграция `a2b3c4d5e6f7` (projects, shares, tasks.project_id). Проверка в проде с коллегой — частично (Нурлан 04.09 создал через Claude ~30 задач) |
| v0.6.1 Понятные ошибки MCP | ✅ в git (`df6895b`) |
| v0.7 Чеклист + перенос задач между проектами | ✅ в git (`cbbea7f`); в проде проверить `alembic current` = `b3c4d5e6f7a8` |
| Адверсариальное ревью (сессия 7) | ✅ `claude/REVIEW-2026-09-04.md`, `claude/review/*.md` — все находки К/В/С и почти все Н закрыты в v0.8 |
| **v0.8 починка** | ✅ код + миграция `c4d5e6f7a8b9` + тесты: pytest **147 passed**, vitest **61 passed**, `npm run build` чисто, сквозной сценарий на стенде пройден. **Ждёт: секреты в Railway/.env, `alembic upgrade head`, коммит/пуш, проверка в проде** |
| **v1.2 Майндмап: цвета, толщина, важность, подписи, связи** | ✅ `api.ts` (`MindNode.color/width/note/priority`, `MindLink`, `root.links`), `MindMapEditor.tsx` (панель узла в экранных координатах, `strokeFor`, `linkGeom`, ручка-связь через pointer capture, `withoutNode`), `styles.css` (блок v1.2, на телефоне панель внизу), версия 1.2; `__tests__/mindmap.test.tsx` 5 тестов, vitest **77 passed**, `vite build` чисто, скриншоты 1440/390 проверены. Без миграции. **Ждёт: `npm run build`, коммит/пуш, проверить на телефоне жест связи** |
| **v1.3 Переезд из Trello — `create_tasks_bulk`** | ✅ `app/mcp_tools.py` (`t_create_tasks_bulk`, `_resolve_containers`, `_owner_for_new_task`, `_new_task`, `_bulk_checklist`, `BULK_MAX=50`), инструкции сервера, `docs/TRELLO.md`, раздел v1.3 в `docs/MCP_CONNECTOR.md`, `test_mcp.py` на относительных датах; версия 1.3 в `main.py`/`Sidebar.tsx`. pytest **266 passed**. Без миграции. **Ждёт: `npm run build`, коммит/пуш; прогон на настоящей доске коллеги** |
| **v1.1.1 Имена кириллицей ↔ латиницей (MCP)** | ✅ `app/names.py` (фонетический ключ, `person_matches`), `mcp_tools.py` (`_match(..., fuzzy=)`, `resolve_person`, `_similar_people`, `create_person`, подсказки и инструкции), версия 1.1.1 в `main.py`/`Sidebar.tsx`; `tests/test_v111_names.py` 49 тестов, pytest **258 passed**, `test_mcp.py`/`test_v06.py` ALL OK. Без миграции. **Ждёт: коммит/пуш, проверить голосом в проде** |
| **v1.1 Потянуть-обновить (телефон)** | ✅ `src/PullToRefresh.tsx` (хук + индикатор), `store.refresh()` без экрана «загрузка…», `update.updatePending()`, правки `App.tsx`/`styles.css`; тест `pull.test.tsx` (5); vitest **72 passed**, `npm run build` чисто, кадры на iPhone 13 просмотрены. Версия 1.1 в `main.py` и сайдбаре. **Ждёт: коммит/пуш, проверка жеста на телефоне** |
| **1.0 Запуск** | ✅ версия 1.0 в API и панели; иконки `favicon/icon-192/icon-512/apple-touch-icon/icon-maskable-512` (локап знак + PLANNER), `index.html` (`apple-mobile-web-app-title` = CIS Planner) и манифест в `vite.config.ts`; презентация 21 слайд (PDF + .pptx у владельца); база знаний для проекта «Как пользоваться Планнером» — 7 файлов, отданы архивом. **Ждёт: `npm run build`, коммит/пуш, переустановка иконки на телефоне** |
| **v0.11 Телефон** | ✅ код + 5 тестов: `src/mobile.ts`, `src/MobileNav.tsx`, правки `App.tsx`, `Board.tsx`, `TaskPanel.tsx`, `Sidebar.tsx` (v0.11), `styles.css` (блок v0.11 в конце), `index.html`; `src/__tests__/mobile.test.tsx`. `npm run build` чисто, vitest **66 passed**, скриншоты 390×844 и 1440 проверены. Один фронт и один сервис Railway — второй фронтенд не нужен. **Ждёт: коммит/пуш, проверка на телефоне, добавить на экран «Домой»** |
| **v0.10 Гости (внешние участники)** | ✅ код + 34 теста (включая пароли): `app/guests.py`, `routers/guests.py`, вход `/api/auth/guest/request|verify`, раздел «Гости» в UI, блок «Я внешний участник» на входе, `docs/GUESTS.md`. Вход гостя — почта + пароль (ссылка только для первого входа и сброса), смена пароля гасит прежние сессии, владельцу уведомление. Прогон: pytest **209 passed**, vitest 61, `npm run build` чисто; экран входа, установка пароля, вход паролем, неверный пароль и раздел «Гости» проверены в браузере. **Ждёт: `alembic upgrade head` (две миграции → `e6f7a8b9c0d1`), коммит/пуш** |
| **v0.9 Telegram-бот отвечает** | ✅ код + 17 тестов: `telegram_link.py`, `routers/telegram.py` (+ меню команд `setMyCommands` при старте), кнопка «Подключить Telegram» в профиле, `docs/TELEGRAM.md`; 20 тестов. **Ждёт: `TELEGRAM_BOT_USERNAME=cisplannerbot` и `TELEGRAM_WEBHOOK_SECRET` в Railway, один запрос `setWebhook`, коммит** |
| **v0.9 Коннектор и в ChatGPT** | ✅ сессия 10: `chatgpt.com`/`openai.com` в `REDIRECT_HOST_ALLOWLIST` (`routers/mcp_oauth.py`), тесты `test_v09_allowed_clients_can_register` / `test_v09_lookalike_hosts_still_rejected` в `test_adversarial_mcp.py`, раздел v0.9 в `docs/MCP_CONNECTOR.md`. Правка только добавлением — Claude и выданные токены не затронуты, похожие домены (`chatgpt.com.evil.com`) отклоняются. pytest **155 passed** отдельно, **172 passed** вместе с Telegram-ботом. Закоммичено `a714f19`. **Ждёт: подключить коннектор в ChatGPT и пройти OAuth** |
| **Презентация для коллег (сессия 8)** | ✅ HTML-деск (артефакт claude.ai) + .pptx с заметками + PDF для телефона. Структура и цифры — `claude/PRESENTATION.md`. В репо файлы не кладём |
| Вход коллег в Entra | ✅ «Требуется назначение?» = Нет — входит любой @cis.kz |
| Каналы в проде | ✅ Telegram, email и календарь Outlook через Graph проверены. Секрет Graph был вставлен в чат — стоит перевыпустить. |

## Следующий шаг (по порядку)
0. **Владелец — v1.3 (Trello):** коммит/пуш (фронт — только цифра версии, `npm run build`). После деплоя открыть в Claude новый чат и сказать «что нового умеет планнер» — в списке инструментов должен быть `create_tasks_bulk`. Отдать коллеге `docs/TRELLO.md`, попросить пройти на одной небольшой доске; если Claude что-то разобрал не так — прислать JSON доски, доразобрать. Файл положить в проект claude.ai «Как пользоваться Планнером».
0. **Владелец — v1.2 (майндмап):** в `frontend` `npm run build` и `npm test` (77 passed), коммит/пуш. После деплоя: открыть майндмап, выбрать узел — над ним панель (цвет, толщина, ! !! !!!, Подпись, Связать); потянуть точку под узлом на другой узел — пунктирная стрелка; клик по стрелке — её панель (цвет, подпись, Убрать).
0. **Владелец — v1.1.1 (имена):** коммит/пуш. После деплоя сказать Claude «поручи <задача> Айдосу» для человека, записанного латиницей — должен найти без дубля; при двух похожих спросит, кого имели в виду.
0. **Владелец — v1.1 (потянуть-обновить):** в `frontend` `npm run build` и `npm test` (72 passed), коммит/пуш. После деплоя на телефоне на Карте направлений или «Задачах» потянуть список вниз из самого верха: выезжает кольцо, при отпускании крутится, затем галочка, данные перечитаны без перезагрузки. Если ждала новая версия сайта — страница перезагрузится на неё.
0. **Владелец — выпуск 1.0:** в `frontend` `npm run build` (иконки уйдут в `dist`), затем `alembic upgrade head` при живой базе (или дать Railway применить миграции при старте), коммит/пуш. На телефоне удалить старую иконку с домашнего экрана и добавить заново — подпись станет «CIS Planner», иконка — лого с PLANNER. Создать проект claude.ai «Как пользоваться Планнером» по `00-ИНСТРУКЦИИ-ПРОЕКТА.md` из архива базы знаний и раздать коллегам ссылку вместе с письмом-инструкцией.
0. **Владелец — v0.11 (телефон):** в `frontend` `npm run build` и `npm test` (66 passed), затем коммит/пуш вместе с v0.10. После деплоя на телефоне открыть `https://cis-planner.up.railway.app` → добавить на экран «Домой» (Safari: «Поделиться» → «На экран „Домой“»; Chrome: меню → «Добавить на главный экран»). Проверить: внизу 4 вкладки; «Ещё» → список направлений и проектов, «⋯» у направления даёт меню шторкой; «Задачи» → одна колонка «В работе», вкладки статусов сверху; «⋯» на карточке → «→ Готово» меняет статус; карточка задачи — на весь экран, статус/проект/приоритет сразу под названием. Компьютерная версия не менялась.
0. **Владелец — v0.10 (гости с паролями):** `alembic upgrade head` → `alembic current` = `e6f7a8b9c0d1 (head)` (применятся две миграции); `.\.venv\Scripts\python.exe -m pytest tests -q` (209 passed) и `npm run build`; коммит/пуш. После деплоя проверить: раздел «Гости» виден только вам; «+ Гость» с внешней почтой; гость по ссылке задаёт пароль и потом входит почтой + паролем; «Сбросить пароль» присылает ему ссылку; «Закрыть доступ» → его сессия сразу перестаёт работать; вам приходит уведомление о смене пароля. Подробности — `docs/GUESTS.md`.
0. **Владелец — Telegram-бот (v0.9):** распаковать архив сессии 9 в репо; в Railway у `backend` добавить `TELEGRAM_BOT_USERNAME=cisplannerbot` и `TELEGRAM_WEBHOOK_SECRET` (32+ символа, `python -c "import secrets; print(secrets.token_urlsafe(32))"`); после перезапуска один раз открыть в браузере `https://api.telegram.org/bot<ТОКЕН>/setWebhook?url=https://backend-production-830f1.up.railway.app/api/telegram/webhook&secret_token=<СЕКРЕТ>` (ответ `"Webhook was set"`). Проверка: боту `/id` → отвечает chat id; в профиле «Подключить Telegram» → «Запустить» → «Готово, Имя!». Подробности и диагностика — `docs/TELEGRAM.md`.
0. **Владелец — секреты (иначе бэкенд v0.8 не стартует):** в Railway у сервиса `backend` проверить `API_TOKEN` и `SESSION_SECRET` — оба ≥16 символов и не `change-me*`; если менялся `API_TOKEN` — обновить его и в переменных фронта `VITE_API_TOKEN` (если задан) и в Swagger. То же в локальном `backend/.env`.
1. **Владелец:** `alembic upgrade head` → `alembic current` = `c4d5e6f7a8b9 (head)`; `npm run build`; `git add . / commit / push`. В логах Railway backend: `Running upgrade b3c4d5e6f7a8 -> c4d5e6f7a8b9`. На сайте в углу — v0.8.
2. **Проверка в проде (чеклист):** карта проектов → ПКМ на «Без проекта» — своё меню; ПКМ на шапке направления → «Удалить направление…» → диалог с вводом названия, Enter ничего не делает; после удаления тост «Отменить» → направление вернулось; «Корзина» и «Архив» в низу сайдбара; «В архив» у проекта → карточка «В архиве» на странице направления; карточка задачи: набрать текст и сразу Esc — правка сохранилась; открыть одну задачу в двух вкладках, поменять в обеих → во второй сообщение о конфликте; ProjectModal → сменить направление → блок «Перенос» с людьми. Голосом через Claude: «возьми в работу договор» при выполненной задаче «Договор» — должен уточнить, а не переоткрыть.
3. **Показ коллегам (материалы готовы, сессия 8):** деск 20 слайдов (~12 минут), живьём показать перетаскивание карточки, чеклист и фразу голосом с телефона; после показа раздать адрес MCP-коннектора и помочь подключить (2 минуты на человека). Слайд «что можно сказать» — раздатка. Для телефона и рассылки — PDF, а не .pptx. В письме коллегам про Telegram теперь достаточно: бот `@cisplannerbot` → «Запустить», затем в профиле «Подключить Telegram».
3.5. **Коннектор в ChatGPT (v0.9, для коллег с подпиской):** после зелёного деплоя backend — **Settings → Security and login → Developer mode** → **+** → `https://backend-production-830f1.up.railway.app/mcp` → OAuth (@cis.kz). На странице согласия должен быть показан адрес `chatgpt.com`. Нужен план Plus / Pro / Team / Enterprise / Edu. **Голосом из ChatGPT нельзя** — голосовой режим не поддерживает коннекторы, только текстом; голос остаётся у Claude. Шаги и ограничения — `docs/MCP_CONNECTOR.md`, раздел v0.9.
4. Коллегам: переподключить коннектор Claude не нужно (токены живы), но при новой авторизации страница согласия теперь показывает адрес возврата и требует тот же браузер.
5. Дальше по желанию: перестановка пунктов чеклиста, пункт → задача, роли «руководитель/команда», агенты тулов; перевыпустить client secret в Entra.

## Что нового в коде v0.8 (для следующей сессии)
- Бэкенд: `app/trash.py` (корзина: soft/hard delete, restore, `purge_trash`, `/trash`, `/impact`); `scope.alive()`, `is_deleted()`, `orphan_clause()`, `in_my_container()`; `Task.directions`/`Direction.projects`/`Project.tasks` отдают только живые записи (полные — `Direction.all_projects`, `Project.all_tasks`); `ProjectIn.move_mode`/`grant_access_user_ids`; `TaskIn.updated_at` → 409; `Settings` валидирует секреты; `mcp_oauth`: cookie `mcp_auth` + хеш в `McpPendingAuth.scope` (`_pack_scope`), `REDIRECT_HOST_ALLOWLIST`, `/oauth/revoke`, `cleanup_oauth()`; `scheduler.housekeeping()` раз в сутки; `mcp_tools`: `_norm`, `_str`, `parse_int`, `_alive`, `warning`/`skipped` в ответах, `get_overview.undelivered_reminders`.
- Фронт: `layers.ts` (`useEscape`, `useDirtyFlag`, `layerCount`), `deletion.tsx` (`useDeletion`: impact → confirm → тост → restore), `toast.tsx`, `confirm.tsx` (`typeToConfirm`, `details`), `Trash.tsx`, `Archive.tsx`, `MiniMenu.tsx`, `update.ts`; `api.ts`: `toIn`, `errorText`, типы `Impact/TrashOut/AccessPreview/MoveMode`. Задача с `project_id`, которого нет в `store.projects`, везде считается «без проекта» («проект в корзине»).
- Стенд: `python _serve_ui.py 8000` (секреты уже длинные), сквозной сценарий — `review/smoke.py` вне репо.

## Тесты (для следующей сессии)
- Бэкенд: `backend/tests/conftest.py` — sqlite во временной папке, `PRAGMA foreign_keys=ON`, фикстуры `client`, `jack` (владелец), `nur`, `aida`, помощник `api`; секреты тестов — константы `API_TOKEN`/`SESSION_SECRET` в conftest. Имена `test_<Уровень><№>_*` соответствуют находкам в `claude/review/backend_*.md`; `test_ok_*` — регрессия; `test_v08_*` — корзина, перенос проекта, MCP/планировщик v0.8. Сейчас все 147 зелёные. Запуск из `backend/` в venv: `python -m pytest tests -q` (`-k K1`, `-k test_ok_`, `-x --tb=short`).
- Фронт: `frontend/vitest.config.ts` (jsdom, `TZ=America/New_York`, чтобы ловить ошибки пояса), `tsconfig.test.json`; тесты в `src/__tests__/`, фикстуры стора — `fixtures.ts`. `npm test` (61 зелёных, `v08.test.tsx` — корзина/тост/перенос); один файл — `npx vitest run src/__tests__/confirm.test.tsx`.
- В среде Claude: `pip install -r requirements.txt pytest`, `npm ci`; оба прогона < 30 с.

## Телефон (v0.11, для следующей сессии)
- Порог «телефон» — `(max-width: 900px)` в двух местах: `MOBILE_QUERY` (`src/mobile.ts`) и медиазапрос в `styles.css` (блок v0.11 в конце) — менять синхронно. Хук `useIsMobile()` даёт false в jsdom (нет `matchMedia`); в тестах `matchMedia` подменяется (`mobile.test.tsx`).
- `MobileNav` не дублирует панель: внутри шторки рендерится обычный `Sidebar` с теми же пропсами; `onView`/`onNewDirection`/`onNewProject`/`onProfile` обёрнуты так, чтобы закрывать шторку. z-index: шторка 15 < модалки 20 < контекстные меню 40 — меню и окна, вызванные из шторки, ложатся поверх неё.
- Доска: `filter` стартует с `in_progress` на телефоне; `.board.single` — одна колонка, заголовок колонки скрыт. Смена статуса — `card-more` («⋯») → `MiniMenu` через `createPortal(document.body)` (fixed внутри карточки с `transform` ломается). Клик по меню не открывает карточку — обёртка `stopPropagation`.
- Карточка задачи: блок `props` (статус/проект/приоритет/дедлайн/проверка) рендерится в `tm-side` на компьютере и между двумя `fieldset.tm-fs` на телефоне — чтобы `disabled` поручённой задачи не блокировал статус исполнителю. Стили `fieldset:is(.tm-main,.tm-fs):disabled`.
- Контекстные меню на телефоне — `.ctx-menu` шторкой снизу через `!important` на `left/top` (позиция считается в JS для компьютера).
- «Потянуть-обновить» (v1.1): жест висит на `.main` (это и есть скролл-контейнер на телефоне), а не на `window`; браузерного жеста нет и не будет — `overscroll-behavior` выключен намеренно. Обновление данных — `store.refresh()` (без `loading`), полная перезагрузка только если ждёт новая версия и нет несохранённых форм. Порог 64px, слушатели пассивные. Тест — `pull.test.tsx` (touch-события на элементе с `scrollTop`).
- Стенд для скриншотов телефона: `backend/_serve_ui.py 8000` + Playwright с `viewport 390×844, is_mobile, has_touch` (скрипт `_shots_mobile.py` — вне репо, в среде Claude).

## Что в UI (v0.2–v0.6)
- **Правило владельца: никаких системных окон браузера** (`window.prompt/confirm/alert`). Создание задачи — поле прямо в колонке (Enter добавляет, Esc закрывает); подтверждения — свой диалог `confirm.tsx` (`useConfirm()` через `ConfirmProvider` в `main.tsx`).
- Доска вписывается в ширину экрана: `grid-template-columns: repeat(var(--cols), minmax(0,1fr))`; на телефоне — горизонтальная прокрутка.
- Кнопки: акцентная (`.btn.primary`) — ржавая, у всех кнопок/чипов/карточек есть hover-реакция; `prefers-reduced-motion` учитывается. Кнопки «+» круглые залитые акцентные.
- **Карта направлений** (`Overview.tsx`, стартовый экран): карточка на каждое направление — шкала внимания («долг внимания» 0–100 → В фокусе / Норма / Ослабло / Упущено), причины, полоса состава задач, счётчики, последнее движение, топ-5 открытых задач. Формула — в `buildReport()`; на бэкенде — `digest.py` (`build_report`). Менять синхронно.
- Сериф (Source Serif 4) — на названиях задач, людей, тулов и заголовках; Rubik — контролы и текст. Оформление: `styles.css` — базовые правила + блок `[data-theme="journal"]`.
- Левая панель (v0.4–v0.6): сверху разделы — Карта направлений, Мне поручено, Общие, Все задачи, Майндмапы, Люди, Тулы; ниже группа «Направления» (сворачивается, фильтр при >8), направления раскрываются в проекты, «+ проект», пометка ⇄ у открытого мне. Правая кнопка / «⋯» на направлении и проекте — меню действий (`DirectionMenu.tsx`, `ProjectMenu.tsx`).
- Клик по направлению → карта проектов (`DirectionPage.tsx`) + блок «Без проекта»; клик по проекту → канбан проекта. Шапка доски — крошки «Направление › Проект», фильтры статусов — вкладки с подчёркиванием.
- Доска: 4 колонки по статусу, drag-and-drop, «+» в колонке создаёт задачу сразу в этом статусе.
- Карточка задачи — **модальное окно по центру** (`.task-modal`, две колонки; Esc / клик мимо — закрыть; на телефоне — на весь экран). Автосохранение через 0.6 с. Поле «Проект» (выбор проекта сам добавляет его направление). При `view` — только-просмотр (`fieldset disabled`, жёлтая шапка). Кнопка ⇄ Поделиться.
- Направление: модалка (название, цель, описание, цвет из палитры, статус). Люди и тулы — таблицы с редактированием. Страницы-списки — колонка по центру ≤1080px.
- `ShareModal.tsx` — почта с подсказками из `/shares/people`, право Смотреть/Редактировать, список и отзыв. `SharedPage.tsx` — раздел «Общие».

## MCP-коннектор (для следующей сессии)
- Схема как в CIS Platform. Claude регистрируется сам (`/oauth/register`), `/oauth/authorize` создаёт `McpPendingAuth` и уводит в Entra со state `mcp:<key>`; `/api/auth/callback` при таком state записывает `user_id` в pending и редиректит на `/oauth/consent?k=`; «Разрешить» → `McpAuthCode` → `/oauth/token` (PKCE) → `McpToken` (хеши). Refresh ротируется.
- Инструменты — `mcp_tools.TOOLS` (name, description, inputSchema, handler). Добавить инструмент = функция `t_<имя>(db, user, args)` + запись в `TOOLS`. Сущности резолвятся `_match()` по id/названию/части/словам. Записи через Claude помечены в `activity_log` `{"via": "mcp"}`.
- **Ошибки (v0.6.1):** `ToolError(message, hint)` → Claude получает `isError` с `{"error", "hint"}`. Подсказки «не найдено» по типу сущности — словарь `_MISSING_HINT`; неоднозначность — «уточните у человека»; дубликат проекта — «используйте существующий»; внутренние сбои — traceback в лог + hint «сообщите человеку». Инструкции сервера (`instructions_for`) велят следовать hint и соблюдать порядок направление → проект → задача.
- **Терпимость к формату (v0.6.1):** `_list(a, key)` — строка вместо массива → `[строка]` (`directions`, `assign_to`, `people`, `add/remove_directions`, `tasks`, `channels`); `arguments` JSON-строкой разбираются; каналы `calendar/outlook/mail/почта/tg` нормализуются. `create_task` понимает `create_direction_if_missing`, `create_project_if_missing` (ровно одно направление), `create_person_if_missing`.
- **Логи:** `INFO mcp: mcp tools/call <tool> by <email> -> ok|error` (роутер) + `WARNING mcp: <tool> by <email>: <текст> | args={…}` (mcp_tools). В Railway фильтр `WARNING mcp`. За 03–04.09 было 125 вызовов, 4 ошибки.
- Права: владелец задачи — всё; исполнитель — `set_task_status` и `update_delegation` (status/report) по своим поручениям; `list_tasks scope=assigned_to_me` — «мне поручено». Удаления нет намеренно. Майндмапы через MCP пока не выведены.
- Инструменты чеклиста (v0.7): `add_checklist_items(task, items[])`, `check_item(task, item, done)`.
- **Пачка задач (v1.3):** `create_tasks_bulk(tasks[], directions?, project?, create_*_if_missing?, skip_duplicates=true, dry_run=false)`. Всё-или-ничего: любая `ToolError` внутри пачки перебрасывается с префиксом `tasks[i] «title»` и хвостом hint «ничего не записано» — откат делает `call_tool`. Дубли ищутся по `_norm(title)` среди `visible_tasks` в том же проекте / тех же направлениях без проекта / без направлений. `_new_task` — общий с `create_task` (без commit), `_bulk_checklist` не режет по запятым. Лимит 50 ↔ `MAX_BODY_BYTES` 256 КБ в `routers/mcp.py`. Инструкция коллегам — `docs/TRELLO.md`; синтетический экспорт Trello и преобразование «как Claude» — в `tests/test_v13_bulk.py` (`TRELLO`, `trello_to_bulk`).
- Идея (владелец, 2026-09-03): роли/видимость для руководителей — отдельное решение по модели прав, не начато.
- Проверка: `python test_mcp.py` и `python test_v06.py` из `backend/` (sqlite, без сети).

## Чеклист и перенос задач (v0.7, для следующей сессии)
- `Task.checklist` — JSON-список `{id, text, done}`; id генерируется на клиенте (`newNodeId()`), на бэкенде в MCP — `secrets.token_hex(4)`. Порядок пунктов — порядок в списке; перестановка пунктов пока не сделана (идея).
- Правка чеклиста идёт тем же `PUT /tasks/{id}` (поле `checklist` в `TaskIn`) — через автосейв карточки. Исполнитель (`assignee`) чеклист в UI не правит (режим только-просмотр); через MCP `check_item` ему разрешён.
- Меняя JSON-колонку на бэкенде — всегда присваивать новый список (не мутировать словари внутри), иначе SQLAlchemy не запишет UPDATE.
- Перенос задачи между проектами: `DirectionPage.moveTask` → `PUT /tasks/{id}` с полной карточкой (`toIn`) и новым `project_id`; `tasks._apply` добавляет направление проекта. Drop-цели — `DropCard` (проекты с правом edit и «Без проекта»); `dataTransfer` тип `text/task-id` — тот же, что на доске.
- Идеи от коллег: перетаскивание задач в проекты из сайдбара; перестановка пунктов чеклиста; превращение пункта в задачу.

## Проекты и совместный доступ
- При выдаче доступа получателю уходит «⇄ Вам открыли …» (`routers/shares.py: share_notice / notify_share`) — Telegram, если есть chat id, иначе письмо.
- Права считаются в `backend/app/scope.py`: `Grants(db, user)`; `task_access` → owner | edit | view | assignee | None; контейнеры могут быть `via`. Фронт получает `access` в каждом объекте (`canEdit()` в `api.ts`).
- Задача, созданная редактором в чужом направлении/проекте, принадлежит владельцу направления (`tasks._owner_for_new`). Напоминания и поручения — owner/edit; удаление и шаринг — только owner.
- `store.tasks` (доска) = мои + открытые мне (+ порученные мне в открытом мне контейнере); `store.inbox` = порученные мне чужие.
- Стенд для скриншотов: `cd backend && python _serve_ui.py 8000` (sqlite с демо-данными, фронт из `frontend/dist`) → `python _shots.py`. В git не нужны.
- Миграцию на sqlite целиком не прогнать (init-миграция Postgres-специфична) — только `alembic upgrade head` у владельца на Docker-Postgres.

## Многопользовательский режим
- Служебный `X-API-Token` = владелец (`OWNER_EMAIL`). Пользовательские сессии — JWT HS256 (`SESSION_SECRET`, 30 дней) в `localStorage['planner.session']`; 401 → экран входа.
- `Person` — общий справочник; при входе запись `Person` с его почтой связывается (`user_id`) или создаётся. Удалять людей с `user_id` нельзя.
- Каналы: Telegram — `User.telegram_chat_id`, почта — `User.email`; календарь — в ящик владельца задачи. Уведомление «Вам поручено» уходит исполнителю один раз (`assigned_notified_at`).

## Майндмапы
- `MindMap.data = {id:"root", text, children:[{id, text, children, collapsed?}]}`; раскладка на фронте (`layout()` в `MindMapEditor.tsx`).
- У направления кнопка: 0 карт → создаёт; 1 → открывает; >1 → список. У задачи — раздел в окне задачи.
- Идеи: перетаскивание узлов между ветками, заметки/ссылки на узле, экспорт в PNG, узел → задача.

## Как устроены напоминания
- Поручения: `Delegation.notified_at IS NULL AND check_at <= now AND status=open` → сообщение владельцу → `notified_at`. Лог `Delegation/check_reminder`.
- Сводка: `digest_due()` — время ≥ `DIGEST_TIME` по `APP_TIMEZONE` и сегодня ещё не было `Digest/sent`. Ручная — `sent_manual`.
- `Reminder.recipient`: `owner` / `assignees` / `both`. `Reminder.sent_at IS NULL AND fire_at <= now` → `deliver()`; при успехе `sent_at`; >24 ч неудач — `gave_up`. Результаты — в `activity_log`.
- `outlook_calendar`: событие 30 мин, id в `Task.outlook_event_id`, повтор — PATCH. Сообщение содержит ссылку `FRONTEND_URL/?task=ID`.

## Правила работы в папке владельца (Cowork)
- Файлы записывать напрямую через device bridge — но **git не запускать вообще** (даже `git status`/`diff` оставляют `.git/index.lock`, который потом блокирует коммит владельца). Если всё же появился — удалить `.git/index.lock` (в PowerShell: `Remove-Item .git\index.lock`).
- Python-зависимости в локальной VM Cowork ставятся `pip install -r requirements.txt`; тесты гонять на копии в `$HOME`, не в папке владельца.

## Известные шероховатости
- Тесты бэкенда (REST) не написаны — есть только `test_mcp.py` / `test_v06.py`.
- В `agents/` только README.
