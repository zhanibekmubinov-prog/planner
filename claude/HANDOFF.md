# HANDOFF — текущее состояние и следующий шаг

_Обновлено: 2026-09-03, сессия 1 (chat)._

## Состояние
| Что | Статус |
|---|---|
| Локально: Docker Postgres, бэкенд (`uvicorn`), фронт (`npm run dev`) | ✅ работает, направление «Закуп» создано локально |
| Первая миграция `alembic/versions/*_init.py` | ✅ сгенерирована и запушена |
| Railway `postgres` | ✅ |
| Railway `backend` | ✅ https://backend-production-830f1.up.railway.app (`/health` → ok, `/docs` открывается) |
| Railway `frontend` | ⏳ было Failed: Node 18 + EBUSY на `npm ci`. Исправлено (`engines.node>=20`, buildCommand только `npm run build`, `types: ["vite/client"]` в tsconfig). Владелец пушит правку — **проверить, что деплой зелёный** |
| `CORS_ORIGINS` у backend | ⏳ пока `*`; после появления домена фронта заменить на `https://<frontend-домен>` |
| Переменные frontend: `VITE_API_URL`, `VITE_API_TOKEN` | ⏳ проверить, что заданы |

## Следующий шаг (по порядку)
1. Убедиться, что `frontend` на Railway задеплоился и показывает список направлений с прод-бэка (прод-база пустая — создать направление через прод `/docs`).
2. Поправить `CORS_ORIGINS`.
3. Мелкий патч бэкенда: сделать `X-API-Token` security-схемой (`APIKeyHeader`), чтобы в Swagger появилась кнопка **Authorize** — сейчас токен вводится в поле параметра у каждого запроса.
4. **Шаг 2 — фронт MVP**: три экрана (список направлений → канбан задач внутри направления с фильтром по статусу → карточка задачи: направления, тулы, делегирование, напоминания). Минимум дизайна, рабочий поток. Перед стартом UI прочитать skill `frontend-design`.

## Известные шероховатости
- `App.tsx` — заглушка, показывает только список направлений.
- Тесты бэкенда не написаны (в каркасе был только ручной smoke-test через TestClient на sqlite).
- В `agents/` только README.
